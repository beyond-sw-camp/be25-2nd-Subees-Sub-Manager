<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.subees.subscription.payment.model.mapper.CardMapper">
    <insert id="insertPaymentMethod">
        INSERT INTO payment_method (
            user_id,
            card_id,
            custom_card_company,
            card_name)
        VALUES (
                   #{userId},
                   #{cardId},
                   #{customCardCompany},
                   #{cardName}
               )
    </insert>

    <select id="countDuplicateByCardId" resultType="int">
        SELECT COUNT(*)
        FROM payment_method
        WHERE user_id = #{userId}
          AND card_id = #{cardId}
          AND is_active = 1
    </select>



</mapper>