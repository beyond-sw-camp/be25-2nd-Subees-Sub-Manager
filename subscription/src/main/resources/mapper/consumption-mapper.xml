<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.subees.submanager.consumption.model.mapper.ConsumptionMapper">

    <!-- 월 총 소비 금액 -->
    <select id="selectMonthlyTotalAmount" resultType="Integer">
        SELECT COALESCE(SUM(
            CASE
                 WHEN billing_cycle = '1M' THEN price
                 WHEN billing_cycle = '1Y'
                 AND MONTH(start_date) = MONTH(#{monthStart}) THEN price
                 ELSE 0 END), 0)
        FROM add_subscription
        WHERE user_id = #{userId}
        AND start_date &lt; #{nextMonthStart}
        AND (end_date IS NULL OR end_date &gt;= #{monthStart})
    </select>


    <!-- 달력 전체 조회 (결제일, 해당 일 결제건수, 해당 일 총 결제금액, 시작일 < 다음 달 시작일,    -->
        <select id="selectCalendar" resultType="com.subees.submanager.consumption.model.dto.CalendarResponseDto">
            SELECT
                LEAST(DAY(a.start_date), DAY(LAST_DAY(#{monthStart}))) AS payDay,
                COUNT(*) AS totalCount,
                COALESCE(SUM(a.price), 0) AS totalAmount,
                GROUP_CONCAT(s.item_name ORDER BY s.item_name SEPARATOR ', ') AS itemNames
            FROM add_subscription a
                     JOIN subscription_item s ON a.item_id = s.item_id
            WHERE a.user_id = #{userId}
              AND a.start_date &lt; #{nextMonthStart}
              AND (
                a.billing_cycle = '1M'
                    OR (a.billing_cycle = '1Y'
              AND MONTH(a.start_date) = MONTH(#{monthStart})))
              AND (a.use_yn = 'Y' OR (a.use_yn = 'N'
              AND a.end_date IS NOT NULL
              AND STR_TO_DATE(
                      CONCAT(
                          DATE_FORMAT(#{monthStart}, '%Y-%m-'),
                LPAD(
                LEAST(DAY(a.start_date), DAY(LAST_DAY(#{monthStart}))),
                2,'0')),
                '%Y-%m-%d'
                ) &lt;= DATE(a.end_date)))
            GROUP BY LEAST(DAY(a.start_date), DAY(LAST_DAY(#{monthStart})))
            ORDER BY payDay
    </select>
    <!-- 선택 일 구독 조회(구독명, 가격, 선택일)-->
    <select id="selectDateDetailList" resultType="com.subees.submanager.consumption.model.dto.DateDetailResponseDto">
        SELECT
            s.item_name AS itemName,
            a.price AS price,
            LEAST(DAY(a.start_date), DAY(LAST_DAY(#{monthStart}))) AS billingDay,
            a.payment_id AS paymentId,
            COALESCE(c.card_company, p.custom_card_company) AS cardCompany,
            p.card_name AS cardName
        FROM add_subscription a
                 JOIN subscription_item s ON a.item_id = s.item_id
                 LEFT JOIN payment_method p ON a.payment_id = p.payment_id
                 LEFT JOIN card c ON p.card_id = c.card_id
        WHERE a.user_id = #{userId}
          AND a.start_date &lt; #{nextMonthStart}
          AND LEAST(DAY(a.start_date), DAY(LAST_DAY(#{monthStart}))) = #{startDate}
          AND (
            a.billing_cycle = '1M'
                OR (
                       a.billing_cycle = '1Y'
                           AND MONTH(a.start_date) = MONTH(#{monthStart})))
          AND (a.use_yn = 'Y'
            OR (a.use_yn = 'N' AND a.end_date IS NOT NULL
          AND STR_TO_DATE(
                      CONCAT(
                          DATE_FORMAT(#{monthStart}, '%Y-%m-'),
            LPAD(LEAST(DAY(a.start_date), DAY(LAST_DAY(#{monthStart}))),
            2,'0')),
            '%Y-%m-%d'
            ) &lt;= DATE(a.end_date)))
        ORDER BY s.item_name ASC
    </select>

    <select id="selectCategory"
            resultType="com.subees.submanager.consumption.model.vo.CategorySummary">
        SELECT
            c.category_name AS categoryName,
            COALESCE(SUM(
                        CASE
                            WHEN a.billing_cycle = '1M' THEN a.price
                            WHEN a.billing_cycle = '1Y' THEN FLOOR(a.price / 12)
                            ELSE 0 END), 0) AS totalAmount,
            GROUP_CONCAT(s.item_name ORDER BY s.item_name SEPARATOR ', ') AS itemNames
        FROM add_subscription a
        JOIN subscription_item s ON a.item_id = s.item_id
        JOIN subscription_category c ON s.category_id = c.category_id
        WHERE a.user_id = #{userId}
        AND a.start_date &lt; #{nextMonthStart}
        AND (a.end_date IS NULL OR a.end_date &gt;= #{monthStart})
        GROUP BY c.category_id, c.category_name
        ORDER BY totalAmount DESC
    </select>

    <select id="selectMonthlyCategoryAnalysis"
            resultType="com.subees.submanager.consumption.model.dto.CategoryAnalysisResponseDto">
        SELECT
        c.category_name AS categoryName,
        COALESCE(SUM(
                     CASE
                        WHEN a.billing_cycle = '1M' THEN a.price
                        WHEN a.billing_cycle = '1Y' THEN FLOOR(a.price / 12)
                        ELSE 0 END), 0) AS totalAmount,
        COUNT(*) AS subscriptionCount
        FROM add_subscription a
        JOIN subscription_item s ON a.item_id = s.item_id
        JOIN subscription_category c ON s.category_id = c.category_id
        WHERE a.user_id = #{userId}
        AND a.start_date &lt; #{nextMonthStart}
        AND (a.end_date IS NULL OR a.end_date &gt;= #{monthStart})
        GROUP BY c.category_name
        ORDER BY totalAmount DESC
    </select>

    <select id="selectYearlyCategoryAnalysis"
            resultType="com.subees.submanager.consumption.model.dto.CategoryAnalysisResponseDto">
        SELECT
        c.category_name AS categoryName,
                    COALESCE(SUM(
                        CASE
                            WHEN a.billing_cycle = '1M' THEN a.price * 12
                            WHEN a.billing_cycle = '1Y' THEN a.price
                            ELSE 0 END), 0) AS totalAmount,
        COUNT(*) AS subscriptionCount
        FROM add_subscription a
        JOIN subscription_item s ON a.item_id = s.item_id
        JOIN subscription_category c ON s.category_id = c.category_id
        WHERE a.user_id = #{userId}
        AND a.start_date &lt; #{nextYearStart}
        AND (a.end_date IS NULL OR a.end_date &gt;= #{yearStart})
        GROUP BY c.category_name
        ORDER BY totalAmount DESC
    </select>

<!-- 월 분석 -->
    <select id="selectMonthlyPaymentList"
            resultType="com.subees.submanager.consumption.model.dto.MonthlyPaymentResponseDto">
        SELECT
            a.payment_id AS paymentId,
            s.item_name AS subscriptionName,
            sc.category_name AS categoryName,
            COALESCE(p.card_name, c.card_company, p.custom_card_company, '등록 카드') AS paymentCardName,
            CASE
                WHEN a.billing_cycle = '1Y' THEN FLOOR(a.price / 12)
                ELSE a.price
                END AS paymentAmount,
            CASE
                WHEN a.billing_cycle = '1Y' THEN '연간 구독(월 환산)'
                ELSE '매월 구독'
                END AS displayDateLabel,
            CASE
                WHEN a.billing_cycle = '1Y' THEN TRUE
                ELSE FALSE
                END AS isYearly
        FROM add_subscription a
        JOIN subscription_item s ON a.item_id = s.item_id
        JOIN subscription_category sc ON s.category_id = sc.category_id
        LEFT JOIN payment_method p ON a.payment_id = p.payment_id
        LEFT JOIN card c ON p.card_id = c.card_id
        WHERE a.user_id = #{userId}
          AND a.start_date &lt; #{nextMonthStart}
          AND (a.end_date IS NULL OR a.end_date &gt;= #{monthStart})
        ORDER BY
            CASE
                WHEN a.billing_cycle = '1Y' THEN 1
                ELSE 0
                END, s.item_name ASC

    </select>
</mapper>