<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.subees.subscription.notification.model.mapper.NotificationMapper">

    <!-- 알림 등록 -->
    <insert id="insertNotification" parameterType="com.subees.subscription.notification.model.vo.NotificationVO">
        INSERT INTO notification (
            user_id,
            subscription_id,
            is_read,
            is_closed,
            notify_type,
            created_at,
            alarm
        ) VALUES (
                     #{userId},
                     #{subscriptionId},
                     #{isRead},
                     #{isClosed},
                     #{notifyType},
                     NOW(),
                     #{alarm}
                 )
    </insert>

    <!-- 사용자 알림 목록 조회 -->
    <select id="selectNotificationList"
            parameterType="long"
            resultType="com.subees.subscription.notification.model.vo.NotificationVO">

        SELECT
            notification_id AS notificationId,
            user_id AS userId,
            subscription_id AS subscriptionId,
            is_read AS isRead,
            is_closed AS isClosed,
            notify_type AS notifyType,
            created_at AS createdAt,
            alarm
        FROM notification
        WHERE user_id = #{userId}
        ORDER BY created_at DESC

    </select>

    <!-- 읽음 처리 -->
    <update id="updateRead">
        UPDATE notification
        SET is_read = TRUE
        WHERE notification_id = #{notificationId}
    </update>

    <!-- 닫기 처리 -->
    <update id="updateClosed">
        UPDATE notification
        SET is_closed = TRUE
        WHERE notification_id = #{notificationId}
    </update>

</mapper>