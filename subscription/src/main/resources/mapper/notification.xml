<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.notification.mapper.NotificationMapper">

    <resultMap id="notificationResultMap" type="com.example.notification.vo.NotificationVO">
        <id property="notificationId" column="notification_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="isRead" column="is_read"/>
        <result property="isClosed" column="is_closed"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 알림 생성 -->
    <insert id="insertNotification"
            parameterType="com.example.notification.vo.NotificationVO"
            useGeneratedKeys="true"
            keyProperty="notificationId">
        INSERT INTO notification (
            user_id,
            content,
            is_read,
            is_closed,
            created_at
        ) VALUES (
                     #{userId},
                     #{content},
                     #{isRead},
                     #{isClosed},
                     NOW()
                 )
    </insert>

    <!-- 특정 유저 알림 목록 조회 -->
    <select id="selectNotificationsByUserId"
            parameterType="long"
            resultMap="notificationResultMap">
        SELECT
            notification_id,
            user_id,
            content,
            is_read,
            is_closed,
            created_at
        FROM notification
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 읽음 처리 -->
    <update id="updateNotificationAsRead" parameterType="long">
        UPDATE notification
        SET is_read = true
        WHERE notification_id = #{notificationId}
    </update>

    <!-- 닫기 처리 -->
    <update id="updateNotificationAsClosed" parameterType="long">
        UPDATE notification
        SET is_closed = true
        WHERE notification_id = #{notificationId}
    </update>

</mapper>